{
  "name": "flyweight-pattern-demo",
  "version": "1.0.0",
  "description": "Flyweight Design Pattern Implementation",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "prompt-sync": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/prompt-sync": "^4.2.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
